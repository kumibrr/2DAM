import { HoverImgDirective } from './hover-img.directive';

describe('HoverImgDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverImgDirective();
    expect(directive).toBeTruthy();
  });
});
