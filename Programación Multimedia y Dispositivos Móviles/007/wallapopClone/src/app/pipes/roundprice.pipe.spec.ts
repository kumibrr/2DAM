import { RoundpricePipe } from './roundprice.pipe';

describe('RoundpricePipe', () => {
  it('create an instance', () => {
    const pipe = new RoundpricePipe();
    expect(pipe).toBeTruthy();
  });
});
